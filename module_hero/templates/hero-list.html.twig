{{ attach_library('module_hero/hero-style') }}

<h4>{{ title }}</h4>
<ol>
  {% for item in items %}
  <li class="animate__animated {{ random(['animate__bounce', 'animate__flash', 'animate__pulse']) }}">{{ item.name }}</li>
  {% endfor %}
</ol>
<strong>This is coming from custom twig file.</strong>